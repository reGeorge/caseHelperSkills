execution:
  max_memory_mb: 512
  timeout: 60
paths:
  sandbox_dir: d:/Code/caseHelper/sandbox
  skills_dir: d:/Code/caseHelper/skills
  workspace_dir: d:/Code/caseHelper/sandbox/workspace
secrets:
  LARK_APP_ID: ${LARK_APP_ID}
  LARK_APP_SECRET: ${LARK_APP_SECRET}
  SDET_API_TOKEN: ${TEST_PLATFORM_TOKEN}
  SDET_BASE_URL: ${TEST_PLATFORM_URL:-https://sdet.ruishan.cc/api/sdet-atp}
